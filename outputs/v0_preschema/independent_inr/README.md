# v0 — Independent INR training (before metrics schema)

This is the **original 29-subject training**.

- Weights: `<sid>/checkpoint.pt`
- Old metrics: `independent_results.csv`, `run_meta.json`
- Lots of NIfTI maps

**Not** the post-schema tables. For new MAE/RMSE/r + DWI RelMSE on these same weights, see:

`outputs/v1_schema_reeval/independent_inr/`
