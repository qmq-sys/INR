# v1 schema training (empty until you retrain)

New Independent INR training with the post-schema trainer writes here:

`<sid>/{best.pt, maps.npz, metrics.json}`  
`summary.csv` / `aggregate.csv`

This folder is intentionally separate from:

- `v0_preschema/independent_inr` (old training)
- `v1_schema_reeval/independent_inr` (old weights, new scores)
